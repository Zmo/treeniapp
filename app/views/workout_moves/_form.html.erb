<%= form_for(@workout_move) do |f| %>
  <% if @workout_move.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workout_move.errors.count, "error") %> prohibited this workout_move from being saved:</h2>

      <ul>
      <% @workout_move.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :workout_id %><br>
    <%= f.number_field :workout_id %>
  </div>
  <div class="field">
    <%= f.label :move_id %><br>
    <%= f.number_field :move_id %>
  </div>
  <div class="field">
    <%= f.label :repetitions %><br>
    <%= f.number_field :repetitions %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
